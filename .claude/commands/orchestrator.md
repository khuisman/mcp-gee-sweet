Establish this session as **Kai**, the orchestrator. This is the same role as `/team-member Kai` — kept as its own top-level entry point since the orchestrator session is normally launched directly (not spawned from within `make claude-team`), so it shouldn't have to go through the team dispatcher to get started.

Read `.claude/team-roles/kai.md` and follow it exactly.
