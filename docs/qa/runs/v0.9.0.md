# QA Run Gate — v0.9.0

**Release type:** Stable
**Suite required:** **Full Regression** — see scoping audit below
**Status:** ✅ pass complete, bugs filed and routed — sign-off conditional on #687/#688 landing before tagging. Pass ran 2026-09-03 → 2026-09-04.

---

## Pre-flight

- [x] Aziz worktree synced to `origin/develop` @ `2cace10` (step 1); run branch `qa/v0.9.0-release-pass`
- [x] Release-candidate source confirmed: main-checkout servers (`oauth`/`sa`/`kai-oauth`/`kai-sa`) run `756eb89`, whose `src/` tree is byte-identical to `origin/develop` `2cace10` (only `.claude/team-roles/bob.md` differs)
- [x] Prompt-file precondition satisfied — Bob cleared it (2026-09-03): `docs/tools.md` sweep done (#681/#682/#683 → v0.9.1), no open pass-procedure PRs. New this pass: PR #685 (`aziz.md` all-slots rule) is in force by direct user instruction; Bob's merge is not a pass blocker (pass exercises it in situ).
- [x] No lane mid-ticket — 0 open PRs; `ash`/`sky`/`jay`/`kit` worktrees all on `team/<name>`, reset to `2cace10` for the pass
- [x] Fixtures: repo-root `.env` present with all 8 `TEST_*`/`SHARED_DRIVE_ID` keys; identical across all worktrees. `DRIVE_FOLDER_ID` = Shared Drive root on every server.
- [x] Fixture state verified (Sales sheet 6 rows, fixture doc structure) — per shard, at shard start; confirmed clean at pass end too

**Auth method used:** OAuth (6 prefixes) + service account (2 prefixes) — see shard table.

---

## Scoping audit — why Full Regression

Per `docs/qa/runs/README.md` §"Scoped gating".

**1. Enumerate.** `git log v0.8.1..origin/develop` — **388 commits, 239 merged PRs**, 2026-07-04 → 2026-09-03 (~2 months).

**2/3. Behavior changes by domain.** New tools and behavior changes land in *every* domain:

| Domain | Representative changes since v0.8.1 |
|---|---|
| sheets_read | #235-era response cap already shipped; `get_multiple_*` `gather()` (#293) |
| sheets_write | rich-text partial hyperlinks in `update_cells` (#89/#380) |
| sheets_mgmt | `add_data_validation`/`get_data_validation` (#361), `update_borders` (#325), `update_sheet_properties` (#301), `resize_rows`/`resize_columns` (#321), `hide_rows`/`hide_columns` (#311), `duplicate_sheet` (#286), `_get_sheet_id` error propagation (#384/#390, #440) |
| sheets_charts | no new tools; teardown added to test cases (#322) |
| drive_files | `star_file`/`unstar_file` (#139), `restore_file`/`empty_trash` (#138), `create_shortcut` (#141), `md5Checksum` exposure (#274), `list_drives` pagination (#489); **Shared Drive fixture migration (#305)** changes default-folder semantics for `create_*` |
| drive_sharing | `transfer_ownership` (#140), `_SA_LIMITED_TOOLS` split (#447/#507) |
| drive_transfer | `convert` on `upload_local_file` (#188), `convert_markdown` on `sync_folder` (#211) + 3 edge-case rounds (#422/#433/#414), `use_checksum` (#274), `import_csv_to_sheet` (#272), `sync_folder` recursive (#328), dry_run dedup + cap offramp (#518), `upload_local_folder` routed through `_upload_local_file` (#411/#505), restamp-failure `fileId` reporting (#420/#650) |
| drive_activity | `list_file_activity` (Drive Activity API v2) |
| docs_content | **`docs/__init__.py` split into 8 submodules (#372/#371/#570/#576)** — structural; `get_doc_as_markdown` (#300/#591/#599), `create_doc_from_file`/`update_doc_from_file` (#341/#564), native md/HTML image conversion (#333/#502), heading-anchor resolution (#409/#453), blockquote support (#476/#546), `insert_softbreak_paragraph`/`insert_local_images` (#332), `find_in_doc` (#353), `create_named_range`/`create_bookmark` (#337), whitespace/paragraph-boundary parser fixes (#401/#402/#441/#443/#450/#478), inline-image size validation (#400/#562) |
| docs_tables | `merge_table_cells` (#310), colspan/rowspan in nested tables (#287), text-with-nested-table positioning (#276/#282) |
| docs_style | `style_doc_table_cells`/`apply_theme` per-edge borders (#403/#462), `create_paragraph_bullets`/`delete_paragraph_bullets` (#334/#524), `link_url=null` clear (#408/#446), nested-bullet depth emit (#336/#432) |
| docs_layout | no new tools |
| calendar | `create_calendar`/`update_calendar`/`delete_calendar` (#266), `add_calendar_to_list`/`remove_calendar_from_list` (#269), Calendar ACL tools (#158/#455), `add_calendar_acl` scope validation (#458/#616), `list_all_events` (#194/#464), `list_calendar_acl` pagination (#460/#612), `find_free_slots` multi-cal (#491) |
| infra | **mcp v1 → v2 migration (#175/#642)** — cross-cutting, all tools + both MCP resources; **async tool execution refactor (#293)** — cross-cutting; MCP resources `get_context()` fix (#363/#368); runtime-configurable cache TTL (#284); `MAX_TOOL_RESPONSE_CHARS` default 40000→1,000,000 (#519/#556); SA-vs-user ADC in `server://auth-status` (#506/#613) |

**4. Refactors never live-tested since v0.8.1's run:** the `docs/` submodule split, the mcp v2 migration, and the async execution refactor are all structural/cross-cutting and none has had a full live pass.

**5/7. Decision.** Every domain has behavior changes; three separate structural/cross-cutting refactors (docs split, mcp v2, async) sit under the whole tool surface; the fixture environment itself moved to a Shared Drive. This is the README's step-7 fallback outright (">roughly half the domains would need a Domain run anyway" — here it's all of them). **Full Regression**, no scoping. Matches the pre-recorded roadmap decision (`project_v09_roadmap` memory: "QA tier = Full Regression").

---

## Pass-prep applied before the run (this branch)

- **#678** — TC-DOC80 (`docs_content.md`) rewritten: `MAX_TOOL_RESPONSE_CHARS` default is now 1,000,000 (#519), the `TEST_LARGE_DOC_ID` fixture (~54k) can't trip it on a normal server. Case now runs against a low cap (`MAX_TOOL_RESPONSE_CHARS=40000` — purpose-started server or a direct `uv run python3 -c` invocation of the real `get_doc_content`), handled by Aziz outside the shared shard. Stale 2026-07-03 Result marked superseded.
- **#680** — `drive_files.md` TC-D03/D04/D27/D58/D59/D61/D175 reworked for Shared-Drive semantics (`DRIVE_FOLDER_ID` ≠ `TEST_FOLDER_ID` now):
  - D58/D59/D03 — "default folder" / "no parent" now resolve to `DRIVE_FOLDER_ID` (Shared Drive), not personal My-Drive root; checks updated.
  - D61 — moves between two throwaway folders instead of "to My Drive root" (avoids cross-drive-move / ownership-transfer semantics).
  - D04/D175 — SA quota error only fires with an explicit personal-Drive destination now; default path succeeds into the Shared Drive. Checks force the personal-Drive dest; SKIP(environmental) if no such folder available.
  - D27 — **confirmed tool gap:** `list_folders(parent_folder_id=None)` hardcodes `'root' in parents`, ignores `DRIVE_FOLDER_ID`; broken for any Shared-Drive deployment / pure-SA. Filed as [BUG-LF] (see Bugs found).

---

## Shard plan

8 server prefixes, all verified equivalently configured (6 OAuth: `mcp-gee-sweet-{ash,sky,jay,kit,kai-oauth,oauth}`; 2 SA: `mcp-gee-sweet-{kai-sa,sa}`). One prefix per shard, run concurrently. Subagents run live calls and report structured PASS/FAIL/SKIP only — **no subagent edits a tracked file**; Aziz writes all Results.

| # | Shard | Files (order) | Exclusive fixture / isolation | Prefix |
|---|---|---|---|---|
| 1 | Sheets | sheets_read → sheets_write → sheets_mgmt → sheets_charts (serial; incl. TC-R36/R37 re-run) | `TEST_SPREADSHEET_ID` (sole writer) | `kai-oauth` |
| 2 | Docs-content | docs_content (minus TC-DOC80) | `TEST_DOC_ID` + own throwaway docs | `oauth` |
| 3 | Docs-style/tables/layout | docs_style, docs_tables, docs_layout | **own throwaway doc only** — substitute a self-created doc for `{DOC_ID}`, never `TEST_DOC_ID` | `ash` |
| 4 | Drive-files | drive_files | own throwaway child folder under Shared Drive | `sky` |
| 5 | Drive-transfer | drive_transfer | own throwaway child folder; local-fs cases SKIP | `jay` |
| 6 | Drive-sharing+activity | drive_sharing, drive_activity | `TEST_PERMISSION_EMAIL` | `kit` |
| 7 | Calendar | calendar | `TEST_CALENDAR_ID` (independent) | `kai-sa` |
| 8 | Infra | infra (live-testable subset) | — | `sa` |
| — | Concurrency | TC-I24, TC-I02 | 2 subagents + `mkdir` barrier, run last, solo | `kai-sa` |
| — | TC-DOC80 | #678 low-cap method | Aziz direct (`uv run python3 -c`, `MAX_TOOL_RESPONSE_CHARS=40000`) | — |

Shared across all shards: one cache DB per server process (concurrency-safe by design — that's what TC-I02 checks), one `LOG_FILE` (`/tmp/mcp-gee-sweet.log`, interleaved but functional), one Google account for the 6 OAuth prefixes (watch for 429/`rateLimitExceeded` — shards back off + retry). Playwright mutex: `/tmp/mcp-gee-sweet-playwright.lock`, per `docs/qa/run.md`.

---

## Suite checklist

- [x] **Sheets Read** — `sheets_read.md` — 40 PASS / 0 FAIL / 2 SKIP (42)
- [x] **Sheets Write** — `sheets_write.md` — 37 PASS / 1 FAIL / 0 SKIP (38 run; 2 more TCs than originally listed, corrected count)
- [x] **Sheets Mgmt** — `sheets_mgmt.md` — 102 PASS / 0 FAIL / 2 SKIP (104)
- [x] **Sheets Charts** — `sheets_charts.md` — 12 PASS / 0 FAIL / 0 SKIP (12)
- [x] **Drive Files** — `drive_files.md` — 75 PASS / 3 FAIL / 9 SKIP (87 run; remainder covered by the pre-approved-SKIP master list)
- [x] **Drive Sharing** — `drive_sharing.md` — 20 PASS / 7 FAIL / 2 SKIP (29)
- [x] **Drive Transfer** — `drive_transfer.md` — 33 PASS / 1 FAIL / 1 SKIP (35 run; remainder is the 57 local-filesystem pre-approved SKIPs, already covered by `tests/integration/`)
- [x] **Drive Activity** — `drive_activity.md` — 4 PASS / 0 FAIL / 1 SKIP (5)
- [x] **Docs Content** — `docs_content.md` — 167 PASS / 0 FAIL / 1 SKIP / 1 N/A (169; TC-DOC80 run separately, see below)
- [x] **Docs Tables** — `docs_tables.md` — 12 PASS / 0 FAIL / 0 SKIP (12)
- [x] **Docs Style** — `docs_style.md` — 27 PASS / 0 FAIL / 0 SKIP (27)
- [x] **Docs Layout** — `docs_layout.md` — 7 PASS / 0 FAIL / 0 SKIP (7)
- [x] **Infrastructure** — `infra.md` — 11 PASS / 0 FAIL / 19 SKIP (30; includes TC-I24/TC-I02 as 2 of the 11 PASS)
- [x] **Calendar** — `calendar.md` — 76 PASS / 0 FAIL / 1 SKIP (77; includes 6 fixture-identity-sensitive TCs re-verified against the real fixture on OAuth after the SA-substitute shard, per Run history)
- [x] **Concurrency** — TC-I24 (47/50 valid data points, zero cross-contamination), TC-I02 (50/50 clean, zero locking errors) — both **PASS**

**Grand total: 623 PASS / 12 FAIL / 38 SKIP / 1 N/A across 674 TCs** (out of 750 in the 14 files — the ~76-TC gap is the pre-approved-SKIP master list below, kept at its standing disposition rather than a redundant fresh line). Plus 17 Playwright/OAuth re-verifications folded into the totals as the authoritative Result for their TCs (see Run history).

---

## Pre-approved SKIPs

Copied from `docs/qa/runs/README.md` master list, trimmed to v0.9.0 scope. TC-I02/TC-I24 are **no longer standing SKIPs** (#673) — run via the barrier procedure.

| TC | Reason | Coverage / action |
|---|---|---|
| TC-I01, I03, I04 | Cache TTL / DB path / persistence — require restart | ✅ `tests/test_cache.py`; persistence trivially true |
| TC-I05–I12 | Tool filtering / auth variants — require restart | ✅ `tests/test_server.py`; auth in #98 |
| TC-I13, I15 | stdio transport / SSE hot reload — env/manual | Verify once per environment |
| TC-D35 | `search_spreadsheets` forced API error | Genuine gap — #490 (unit test added, PR #572; confirm) |
| TC-D95 | `upload_local_file` skip_if_exists=False duplicate | #495 (PR #634 — confirm) |
| TC-D98–D107, D109, D110 | `upload_local_folder` / `download_file` / `download_folder` params — local-filesystem | Unit coverage added #485/#486/#487; live path is local-fs, SKIP here |
| TC-D119 | `sync_folder` invalid `direction` | #488 (PR #563 — confirm returns error dict) |
| TC-D123 | `list_drives` pagination | #489 (PR #569); also permanently env-skipped per #305 (`TEST_FOLDER_2_ID` retired) |
| TC-D155 | `list_shared_with_me` quote escaping | #494 (PR #577 — confirm) |
| TC-DOC51 | Nested tables in markdown | Documented limitation |
| TC-CAL04 | `list_calendars` empty subscription | Environmental |
| TC-CAL32 | `find_free_slots` multi-calendar merge | #491 (PR #575 — confirm) |

(Per-shard SKIPs beyond this list get recorded with reasons at compile.)

---

## Results

| Run date | Auth | Results file | PASS | FAIL | SKIP | N/A |
|---|---|---|---|---|---|---|
| 2026-09-03/04 | OAuth (6 prefixes) + service account (2 prefixes) | [docs/qa/results/2026-09-04.md](../results/2026-09-04.md) | 623 | 12 | 38 | 1 |

## Run history / incidents

- **Playwright browser wasn't authenticated to Google at pass start.** Every one of the 8 parallel shards independently hit the sign-in redirect within its first few `**Playwright: required**` TCs, concluded Playwright was unusable, and fell back to API-level-only verification for its entire remaining run. The user authenticated Playwright mid-pass; corrections were sent to the two still-running shards (Sheets, Docs-content retries) — one was misdirected due to an agent-ID mix-up on Aziz's own end (caught and corrected). A dedicated re-verification pass then redid the ~17 highest-value already-completed visual checks (10 docs/drive_transfer TCs, 7 Sheets TCs) with real screenshots — all PASS, zero discrepancies from the API-only evidence. Filed **PR #686** to make Playwright-auth verification (a real navigate, not just a connection check) a mandatory pre-flight step for future passes, done once centrally before spawning shards.
- **Service account wasn't subscribed to the fixture calendar.** The Calendar shard's reads against `{CALENDAR_ID}`/`{EVENT_ID}` all 404'd on the SA prefix; it ran its full 77-TC suite against two SA-owned scratch calendars instead (valid tool-behavior coverage, but not against the real fixture). A targeted OAuth re-run against the real fixture closed the gap for 6 identity-sensitive TCs (CAL05/16/60/62/67) plus 2 that only work under OAuth at all (CAL17/22 — attendee invites, which a service account cannot send) — all PASS, real fixture confirmed untouched (`updated` timestamp identical pre/post).
- **Cross-shard fixture interference (transient, resolved).** The Sheets shard's writes to `{SPREADSHEET_ID}` were caught mid-mutation by the Drive-transfer shard's CSV-export comparison (TC-D86 vs TC-D104 diverged: extra column, an `#ERROR!` cell, an extra row). Re-checked after the Sheets shard restored the fixture to seed state — both exports now agree byte-for-byte. Not a product bug; a sharding-design lesson (two shards touching the same fixture, one read-mostly, without wave-sequencing).
- **Main-checkout OAuth `token.json` refresh token is failing (`invalid_grant`) for fresh processes.** Already-running MCP server processes are unaffected (they hold an already-refreshed in-memory token — confirmed live-working throughout this pass), but any *new* script/session using this file will hit it and fall into an interactive-consent hang. This blocked one live re-verification (TC-DOC159 — raw `documents().get()` read of blockquote indent/border), disposed instead via a source-diff argument (the exact code path hasn't changed since its last live PASS). Recommend re-running `scripts/oauth_setup.py` before the next session needs a fresh OAuth connection — flagged to the user, not a QA finding.
- **Two subagent-prompt mistakes, both self-inflicted and non-blocking:** the TC-I24/TC-I02 concurrency subagents were given a slightly wrong tool-call syntax (`range="Sheet!A1:C3"` instead of separate `sheet=`/`range=` params) for their first 1-2 iterations each; all subagents self-corrected immediately and the remaining iterations are the ones adjudicated. Neither is a transport/locking signature — genuine client-side parameter errors.
- **Google Drive API had two transient server-side blips** (an `export_file` `HttpError 500 Internal Error`, and one `delete_file(permanent=True)` `404` on a file `get_file_metadata` could see and trash could act on — isolated to the service-account prefix specifically; the same delete succeeded immediately via OAuth on the identical file, suggesting a Shared-Drive Content-Manager-role permission-model quirk rather than a code defect). Neither reproduced on retry via the correct auth path; not filed as product bugs.

---

## Bugs found

| ID | Title | Severity | Disposition |
|---|---|---|---|
| BUG-SS | `share_spreadsheet` omits `supportsAllDrives=True` on its `permissions().create()` call — every sibling permission op in the same file has it. Fails with "File not found" for any Shared-Drive file (the current fixture environment, post-#305). `share_file` still works (functional workaround). | Real, release-scope | File — v0.9.0, route to Dev lane B |
| BUG-CACHE | Folder-listing cache key omits `max_results` — a smaller-limit call can (a) get an unfiltered cache hit from an earlier larger call, ignoring the limit, and (b) *store* its own truncated result, so later larger-limit calls get the truncated list until `refresh_cache`. Reproduced twice. Affects `list_files`, `list_spreadsheets`, `list_folders` (shared cache). | Real, release-scope — silent persistent wrong data, the most severe finding this pass | File — v0.9.0, route to Dev lane B |
| BUG-LF | `list_folders(parent_folder_id=None)` hardcodes `'root' in parents`, ignores `DRIVE_FOLDER_ID` — broken scope on any Shared-Drive deployment and for a pure service account (no personal root). Confirmed live (TC-D27). Has an explicit-parent workaround. | Real, confirmed live | File — v0.9.1 |
| BUG-DF | `download_file` with a trailing-slash `local_path` to a non-existent directory creates a single file at that path (not the directory) and silently clobbers it on every subsequent download to the same path. Correct once the directory already exists. | Real, minor | File — v0.9.1 |
| (enhancement) | `create_spreadsheet`/`import_csv_to_sheet` response has no web link, unlike `copy_file`/`create_folder`/`get_file_metadata` | Minor/judgment call | User to decide: add `web_link` for consistency, or fix TC-D01's expectation instead |
| (doc note) | Service account's Shared-Drive "Content manager" role can trash but apparently not permanently-delete (`files().delete()`) an item it created — surfaced as a 404, not 403. Same call succeeds immediately via OAuth on the identical file. Looks like a Drive API permission-model fact, not a code defect. | Minor, informational | Note in `_SA_LIMITATIONS`/docs, not filing as a bug |
| (minor) | `find_free_slots` per-calendar error shape (`{"error": "notFound"}`, bare) is less detailed than `list_all_events`'s (`{calendar_id, calendar_summary, error: <full repr>}`) for the same failure mode | Minor | File — v0.9.1 |
| (minor) | A bare `-` marker alone on its line isn't recognized as a list item by python-markdown regardless of indentation width — distinct from the tracked `sane_lists` threshold bug these fixtures otherwise target | Minor, markdown-syntax-level gap | File — v0.9.1, or a doc note in `markdown-support.md` |

**Filed** (2026-09-04, per direct user go-ahead):

| ID | Issue | Labels |
|---|---|---|
| BUG-SS | [#687](https://github.com/khuisman/mcp-gee-sweet/issues/687) | `defect`, `qa`, `v0.9`, `ready-for-development`, `lane-b` |
| BUG-CACHE | [#688](https://github.com/khuisman/mcp-gee-sweet/issues/688) | `defect`, `qa`, `v0.9`, `ready-for-development`, `lane-b` |
| BUG-LF | [#689](https://github.com/khuisman/mcp-gee-sweet/issues/689) | `defect`, `qa`, `v0.9.1` |
| BUG-DF | [#690](https://github.com/khuisman/mcp-gee-sweet/issues/690) | `defect`, `qa`, `v0.9.1` |
| find_free_slots error-shape | [#691](https://github.com/khuisman/mcp-gee-sweet/issues/691) | `defect`, `qa`, `v0.9.1` |
| markdown bare `-` marker gap | [#692](https://github.com/khuisman/mcp-gee-sweet/issues/692) | `defect`, `qa`, `v0.9.1` |
| `create_spreadsheet`/`import_csv_to_sheet` web_link | [#693](https://github.com/khuisman/mcp-gee-sweet/issues/693) | `enhancement`, `qa`, `v0.9.1` |

BUG-SS (#687) and BUG-CACHE (#688) routed straight to `ready-for-development`/`lane-b` (both, simultaneously — a deliberate exception to the usual one-at-a-time front-of-queue labeling discipline, per direct user instruction: these are two known, already-scoped v0.9.0-blocking findings from this pass, not an untriaged backlog). The service-account Shared-Drive permanent-delete finding is a docs-only note (`_SA_LIMITATIONS`), not filed as an issue.

---

## Sign-off

- [x] All 14 domain suites + concurrency complete, Results written inline (674 TCs)
- [x] TC-DOC80 (#678) re-run under low-cap method — PASS
- [x] All FAILs disposed — 4 are stale test-case assumptions (fixed in this branch: TC-D124, TC-D13, TC-W02, TC-CAL16), 1 (TC-D167) tracked as a follow-up of the same class as TC-DOC80, 6 (the `share_spreadsheet` TCs) and 1 (TC-D40) trace to BUG-SS/BUG-CACHE respectively — filed and routed above. No FAIL is left undiagnosed.
- [x] `docs/qa/results/2026-09-04.md` written and linked
- [x] Bugs filed — #687-#693 (BUG-SS, BUG-CACHE, BUG-LF, BUG-DF + 3 minor)
- [x] QA-gate PR opened — see below

**BUG-SS (#687) and BUG-CACHE (#688) are routed to Dev lane B for a v0.9.0 fix before the release tags** — sign-off is conditional on those landing; v0.9.0 should not ship with them open. BUG-LF, BUG-DF, and the 3 minor findings are lower-severity/have workarounds and are v0.9.1 follow-ups.

**Signed off by:** Aziz (Release QA Lead), pending user confirmation of the final go-ahead to tag once #687/#688 land **Date:** 2026-09-04
