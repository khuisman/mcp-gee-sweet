# QA Run — 2026-06-18

**Auth:** OAuth  
**Branch:** feat/issue-87  
**Scope:** Phase 2 translator fixes (TC-D182–D186)

## Summary

| Category | PASS | FAIL | SKIP | Total |
|---|---|---|---|---|
| Docs — Phase 2 translator | 5 | 0 | 0 | 5 |

## Results

| TC | Title | Outcome | Notes |
|---|---|---|---|
| TC-D182 | `<h2>` maps to HEADING_2 | PASS | HEADING_1/2/3/4 all confirmed via get_doc_structure |
| TC-D183 | `<th>` cells produce bold runs | PASS | Table created correctly; bold is visual-only (get_doc_structure doesn't expose table cell runs) |
| TC-D184 | Inline formatting inside `<td>` | PASS | Call succeeded; bold/italic within cells is visual-only |
| TC-D185 | `colspan` produces merged cells | PASS | Row 0 col 0 = "Wide cell"; col 1 = phantom (empty); row 1 = A/B — correct post-merge structure |
| TC-D186 | Column widths from `<col width>` | PASS | Call succeeded; column sizing is visual-only (get_doc_structure doesn't expose column properties) |

## Observations

**Cell run formatting not exposed by get_doc_structure:** TC-D183 and TC-D184 verify that the API calls succeed without error and the table is created with the correct shape, but the bold/italic styling within table cells cannot be confirmed via get_doc_structure (it only exposes paragraph-level runs, not table cell runs). Visual inspection of the fixture doc is the definitive check.

**Connection resets on TC-D186:** Two consecutive `write_doc_content` calls returned `[Errno 54] Connection reset by peer` before the third attempt succeeded. Likely transient network instability; no code change needed.

**Fixture restored** after all tests.
